# Passo a Passo

- Criei o monorepo com o comando: `npx create-nx-workspace projects --preset=nest`;
- Troquei o gerenciador de pacotes de npm para pnpm;
- Você pode testar a aplicação localmente com o comando: `pnpm nx serve auth-backend`;